# Task: Accessibility (A11y)

## Status: Completed

## Checklist
- [x] **ARIA Roles**: Add `role="img"` (static) or `role="button"` (interactive).
- [x] **ARIA Labels**: Implement the resolution order (`alt` > `Avatar of {name}` > "User avatar").
- [x] **Keyboard Interaction**: Implement `tabindex="0"` and `Enter`/`Space` listeners when `interactive` prop is true.
- [x] **Status A11y**: Add `aria-label` to status indicator.
