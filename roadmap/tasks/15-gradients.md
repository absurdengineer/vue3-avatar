# Task: Fallback Gradients

## Status: Completed

## Goals
Provide premium default look for avatars without images.

## Checklist
- [x] **Utils**: Update `getAvatarColors` to support gradients.
- [x] **Props**: Add `gradient` (boolean) prop (default `false`).
- [x] **Implementation**: Return `linear-gradient(...)` strings for background style.
