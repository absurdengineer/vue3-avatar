# Task: Core Component Structure & Props

## Status: Completed

## Checklist
- [x] **Refactor Folder Structure**: Move `avatar.vue` to `src/components/Avatar.vue` (if not already aligned) and update entry points.
- [x] **Formalize Initials Logic**: Extract initials logic into a pure utility function `getInitials(name)` and unit test it strictly against the v4 contract.
- [x] **Props Review**: Ensure all v4 props are defined with correct types/defaults (Identity, Size, Colors, Status).
- [x] **Custom Style Props**: Implement `customAvatarStyle` and `customStatusStyle` merging logic.
