# 🗺️ Roadmap - vue3-avatar v4.1

## 🚀 High Priority (Committed)

### 1. Enhanced Image Handling

Focus on smoothing the visual transition between the "Initials" state and the "Image Loaded" state.

- [x] **Transition Support**
  - [x] Define standard CSS opacity transitions for the internal `<img>` element.
  - [x] Implement an `onLoad` handler to toggle a `isLoaded` state.
  - [x] Add a `transition` prop to allow customization or disabling of the fade.
- [x] **Native Lazy Loading**
  - [x] Add `loading` prop to the component (types: `'lazy' | 'eager'`).
  - [x] Pass the attribute directly to the underlying `<img>` tag.

### 2. AvatarGroup Interactivity

Improve the `AvatarGroup` component to be functional, not just visual.

- [x] **Overflow Event**
  - [x] Add a click listener to the `+N` count bubble.
  - [x] Emit `@overflow-click` with the list of hidden user objects as the payload.
  - [ ] Update `AvatarGroup` documentation with an example using a Popover/Modal.

## 🧪 Under Consideration (Tentative)

### 3. Advanced Fallback Strategies

Expand fallback logic beyond the current `Image -> Initials` binary.

- [ ] **Fallback Modes:** Investigate adding a `fallback` prop to support alternative failure states.
  - _Ideas:_ `'icon'` (renders a generic user icon/slot) or `'service'` (hooks into external generators).

## 📍 Status Indicator Improvements

- [x] **Positioning Logic**
  - [x] Change default `status` position to `bottom-right`.
  - [x] Add `status-position` prop supporting: `top-right`, `top-left`, `bottom-right`, `bottom-left`.
  - [x] Update CSS variables to handle offset calculations based on avatar shape (circle vs square).

### 4. SSR & Framework Support

Ensure the library is "SSR-Safe" and plays well with Nuxt.

- [x] **Deterministic Colors**
  - [x] Audit the color generation hash to ensure it produces identical results on Server and Client.
  - [ ] Add a section to the docs specifically for Nuxt/Nitro users.

## 🎨 New Visual Feature: "PixelGen"

_A fun, deterministic alternative to Initials._

- [x] **Core Implementation**
  - [x] Create a hashing utility to convert a string into an 8x8 boolean grid.
  - [x] Build an SVG generator that renders rects based on the hash.
  - [x] Add `variant="pixel"` to the main component props.
- [x] **Styling**
  - [x] Implement "Color Themes" for pixels (e.g., 'earth', 'neon', 'ocean').

## 🔌 Ecosystem Integration (The "Nuxt Fix")

- [x] **Scoped Slots**
  - [x] **#image**: Provide `{ src, alt }` to allow using `<NuxtImg>` or custom components.
  - [x] **#placeholder**: Allow custom SVGs/Icons when both image and name are missing.

## ⚙️ Developer Experience

- [x] **Global Configuration**
  - [x] Update the Vue plugin installer to accept a `defaults` object.
  - [x] Use `provide/inject` or a global reactive object to merge local props with global defaults.
- [x] **Auto-Contrast Engine**
  - [x] Implement a YIQ or luminance check on the background color.
  - [x] Automatically switch text color between `#FFFFFF` and `#000000` for readability.

## 📝 Documentation

- [x] Create a "Migration Guide" for v4.0 to v4.1.
- [x] Add interactive examples for `AvatarGroup` events.
- [x] Document the `status-position` and `variant="pixel"` props.
